# AI Context – Escrime Vision System

This project implements an automatic fencing streaming system.

Architecture:

- Vision:
  - piste detection
  - person detection (YOLO)
  - multi-object tracking (ByteTrack)
  - auto-framing

- Streaming:
  - RTSP output
  - software encoder (DEV)
  - NVENC encoder (Jetson)

- Control:
  - FastAPI REST
  - Web interface

Constraints:

- DEV runs in WSL
- PROD runs on Jetson Orin Nano
- Same pipeline code must run in both environments
